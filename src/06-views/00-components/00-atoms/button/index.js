import React from 'react';

import styles from './button.css';

const Button = () => (
  <button
    type="button"
    className={styles.button}
  >
    Hellosss
  </button>
);

export default Button;
